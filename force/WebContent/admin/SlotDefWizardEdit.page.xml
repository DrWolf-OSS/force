<?xml version="1.0" encoding="UTF-8"?>
<page xmlns="http://jboss.com/products/seam/pages"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://jboss.com/products/seam/pages http://jboss.com/products/seam/pages-2.2.xsd"
      no-conversation-view-id="/SlotDefList.xhtml"
      login-required="true">

   <begin-conversation join="true" flush-mode="MANUAL"/>

   <action execute="#{slotDefHome.wire}"/>

   <param name="slotDefFrom"/>
   <param name="slotDefId" value="#{slotDefHome.slotDefId}"/>
   <param name="model" value="#{slotDefEditBean.model}"/>
   <param name="mode" value="#{slotDefEditBean.mode}"/>
   <param name="from" value="#{slotDefEditBean.from}"/>
   <param name="wizard" value="#{slotDefEditBean.wizard}"/>

	<action execute="#{slotDefEditBean.setKnownParameters}"/>

   <navigation from-action="#{slotDefHome.persist}">
      <rule if-outcome="persisted">
         <end-conversation/>
         <redirect view-id="/SlotDef.xhtml"/>
      </rule>
   </navigation>

   <navigation from-action="#{slotDefHome.update}">
      <rule if-outcome="updated">
         <end-conversation/>
         <redirect view-id="/SlotDef.xhtml"/>
      </rule>
   </navigation>

   <navigation from-action="#{slotDefHome.remove}">
      <rule if-outcome="removed">
         <end-conversation/>
         <redirect view-id="#{empty from ? 'SlotDefTemplateList.xhtml' : from}">
         	<param name="model" value="#{slotDefEditBean.model}"/>
    		<param name="mode" value="#{slotDefEditBean.mode}"/>
    		<param name="wizard" value="#{slotDefEditBean.wizard}"/>
         </redirect>
      </rule>
   </navigation>
   
   <navigation from-action="#{slotDefEditBean.save()}">
      <rule if-outcome="persisted">
         <end-conversation/>
         <redirect view-id="#{empty from ? '/admin/SlotDefTemplateList.xhtml' : from}">
         	<param name="model" value="#{slotDefEditBean.model}"/>
    		<param name="mode" value="#{slotDefEditBean.mode}"/>
    		<param name="wizard" value="#{slotDefEditBean.wizard}"/>
         </redirect>
      </rule>
      <rule if-outcome="failed">
         <redirect>
         	<param name="model" value="#{slotDefEditBean.model}"/>
    		<param name="mode" value="#{slotDefEditBean.mode}"/>
    		<param name="wizard" value="#{slotDefEditBean.wizard}"/>
         </redirect>
      </rule>
   </navigation>
   
   <navigation from-action="#{slotDefEditBean.update()}">
      <rule if-outcome="updated">
         <end-conversation before-redirect="true"/>
         <redirect>
         	<param name="slotDefId" value="#{slotDefHome.id}"/>
    		<param name="model" value="#{slotDefEditBean.model}"/>
    		<param name="mode" value="#{slotDefEditBean.mode}"/>
    		<param name="wizard" value="#{slotDefEditBean.wizard}"/>
         </redirect>
      </rule>
      <rule if-outcome="failed">
         <redirect>
         	<param name="slotDefId" value="#{slotDefHome.id}"/>
         	<param name="model" value="#{slotDefEditBean.model}"/>
    		<param name="mode" value="#{slotDefEditBean.mode}"/>
    		<param name="wizard" value="#{slotDefEditBean.wizard}"/>
         </redirect>
      </rule>
   </navigation>

</page>
